# Olist E-Commerce Delivery Delay Prediction Service (Task 3)

An enterprise-grade MLOps production inference service built with FastAPI, Docker, and Scikit-Learn.

## 1. Quickstart (One-Command Deployment)

Make sure Docker Desktop is running, then execute:

```powershell
docker compose up --build -d